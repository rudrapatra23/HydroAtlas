# Public environment template. Copy to .env.local and fill in real values.
# Variables prefixed with NEXT_PUBLIC_ are exposed to the browser bundle.

# Backend API base URL. REQUIRED — lib/config/env.ts fails the build if missing.
NEXT_PUBLIC_API_BASE_URL=https://api.hydraatlas.com
