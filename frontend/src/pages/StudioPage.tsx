import AppShell from "../components/AppShell";

/**
 * Studio page — renders the existing HydraAtlas application.
 * This is a passthrough wrapper: no additional logic, no state, no side-effects.
 */
export default function StudioPage() {
  return <AppShell />;
}
